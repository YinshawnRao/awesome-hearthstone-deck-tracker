<Window x:Class="HearthstoneDeckTracker.App.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hearthstone Overlay"
        Width="260"
        Height="420"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        Background="Transparent">
    <!--
      Click-through / no-activate are not set here.
      TODO: After SourceInitialized, add WS_EX_TRANSPARENT (hit-test skip) and WS_EX_NOACTIVATE
      (do not steal focus from Hearthstone) via SetWindowLong on the HWND.
      TODO: Optionally WS_EX_TOOLWINDOW to hide from Alt+Tab.
      TODO: Follow the Hearthstone client window (size/position); this stub is a free-floating sidebar.
    -->
    <Border Background="#CC141820"
            BorderBrush="#66C9A227"
            BorderThickness="1"
            CornerRadius="4"
            Padding="12"
            Margin="8">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top"
                       Foreground="#C9A227"
                       FontWeight="SemiBold"
                       Text="Deck remaining"/>
            <TextBlock DockPanel.Dock="Top"
                       Margin="0,4,0,12"
                       Foreground="#9AA3B2"
                       FontSize="11"
                       TextWrapping="Wrap"
                       Text="Overlay stub. Card list and click-through are not implemented."/>
            <TextBlock Foreground="#F2F4F8"
                       TextWrapping="Wrap"
                       Text="0 cards — waiting for Power.log state machine + card DB."/>
        </DockPanel>
    </Border>
</Window>
